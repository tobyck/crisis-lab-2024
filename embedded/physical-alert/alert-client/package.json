{
  "name": "alert-client",
  "version": "1.0.0",
  "description": "pip install pyserial pip install websocket-client git clone git@github.com:machinezone/IXWebSocket.git mkdir build && cd build cmake -DUSE_TLS=1 ../IXWebSockets make -j sudo make install",
  "main": "client.js",
  "scripts": {
    "test": "test"
  },
  "author": "Maxwell Robati",
  "license": "ISC",
  "dependencies": {
    "serial": "^0.0.9",
    "serialport": "^12.0.0",
    "ws": "^8.17.0"
  }
}
